#include <stdio.h>
#include <math.h>
int main(){
    int n,i,x,y,z;
    scanf("%d",&n);
    for(i=0;i<n;i++){
            x=i+1;
            y=pow((i+1),2);
            z=pow((i+1),3);
            printf("%d %d %d\n",x,y,z);
    }
    return 0;
}
