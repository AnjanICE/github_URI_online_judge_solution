#include <stdio.h>
main()
{
    int a,T,i=0,j=0;
    scanf("%d",&T);
    while( T-- && scanf("%d",&a)!=EOF)
    if(a>=10 && a<=20)
        {
            i=i+1;
            }
        else{
            j=j+1;
            }
        printf("%d in\n",i++);
        printf("%d out\n",j++);

        return 0;
    }
