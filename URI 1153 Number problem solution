#include <stdio.h>
long int factorial(int n)
{
	if(n==1)
	return 1;
	else
	return(n*factorial(n-1));
	}
	main()
	{
		int n;
		long int f;
		scanf("%d",&n);
		f=factorial(n);
		printf("%d\n",f);
		return 0;

		}
